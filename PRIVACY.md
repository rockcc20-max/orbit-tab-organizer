# Orbit 隐私说明

适用版本：0.7.1。更新日期：2026-09-07。

## 本地处理的信息

Orbit 通过 Chrome 扩展接口读取当前可访问的普通浏览器窗口及其中的标签页，包括标题、网址、标签页与窗口标识、最近访问时间，以及活动、固定、播放声音、加载、休眠、窗口显示状态等信息。这些信息仅用于在本机汇总网站、搜索与排序、切换页面和识别重复项。

总览页打开时，相关信息保存在页面运行内存中，并随标签页与窗口变化更新。Orbit 不读取网页正文、表单输入、密码、Cookie 或完整历史记录数据库，也不在网页中注入内容脚本。

## 网络与第三方

Orbit 没有账号系统、后端服务器、广告、分析统计或遥测服务，不将标签页标题、网址、搜索内容或窗口信息发送给开发者或第三方服务。

网站图标通过 Chrome 自身的 favicon 接口获取；Orbit 不调用第三方图标服务。脚本、样式和后备图标随扩展提供，不从 CDN 加载。浏览器自身以及用户访问的网站可能有独立的网络行为与隐私政策，不属于 Orbit 的控制范围。

## 权限说明

- `tabs`：读取标签页的标题、网址等信息，支持网站汇总、搜索与重复判断；按用户操作切换或关闭标签页。
- `favicon`：通过 Chrome 的图标接口显示对应网站的 Logo。

当前版本未声明额外的网站访问权限、历史记录权限或内容脚本。是否可访问无痕窗口等范围，由 Chrome 的扩展设置决定；重复清理不会将普通与无痕标签页互相判重。

自 0.6.2 起不再申请 `storage` 权限，也不再写入旧版本的默认设置或空快照数组；外观偏好通过页面本地存储保存，不需要该扩展权限。

## 用户控制

未设置手动偏好时，外观跟随浏览器的浅色／深色设置，并显示对应的太阳／月亮图标。点击图标直接切换明暗后，Orbit 仅在本机页面的 `localStorage` 中保存 `orbitTheme` 外观设置，用于下次打开时恢复，并在同一扩展的总览页之间同步；不上传或保存浏览内容。

界面语言默认根据浏览器语言在中文、英文、韩文和日文中选择，并直接显示语言名称；不支持的语言回退为英文。下拉只显示四种语言，手动选择保存在本机 `localStorage` 的 `orbitLanguage` 设置中，用于下次恢复。翻译文本随扩展打包，不发送页面内容给翻译服务。手动同步只重新读取本机当前标签页，不是云端同步。

读取和展示标签页不会自动关闭页面。批量关闭重复项需用户选择并确认，执行前会重新检查页面状态；每行关闭按钮则按用户点击直接关闭对应页面。关闭前请注意未保存的内容。

搜索文字和本次选择不会被 Orbit 持久化。关闭总览页会结束该页的运行内存；卸载扩展可移除其本地扩展存储。当前版本没有云端数据或需要申请删除的服务器记录。

## 数据使用限制

Orbit 对通过 Chrome 扩展接口获取的信息的使用遵守 [Chrome Web Store 用户数据政策](https://developer.chrome.com/docs/webstore/program-policies/user-data-faq)，包括 [Limited Use 要求](https://developer.chrome.com/docs/webstore/program-policies/limited-use)。相关信息仅用于已披露的本地标签页整理功能，不出售、不用于广告、信用评估或借贷，也不向开发者或第三方提供查看浏览内容的渠道。

## 反馈

可通过项目仓库的 Issue 反馈隐私问题。公开提交截图、日志或复现示例前，请移除个人信息、私人网址、访问令牌以及不希望公开的标签页内容。
